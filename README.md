# QABOT Monorepo

This repository currently contains:

- `apps/backend` (`backend`)
- `apps/web` (`@qabot/web`)

## Scripts

- `pnpm install`
- `pnpm dev` (runs workspace dev tasks for remaining apps)
- `pnpm build`
- `pnpm test`

## Notes

- Flamecast apps and CLI have been removed.
